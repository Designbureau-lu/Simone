import { MorphingSurface } from "./MorphingSurface.js";

/**
 * Preserves the existing surface name while using the single fold geometry.
 * Model Transition therefore changes no geometric placement.
 */
export class ContactedFoldSurface extends MorphingSurface {}
